label(for='{ model.name }', if='{ opts.label }')
  | { opts.label }
input(id='{ model.name }', name='{ model.name }', onchange='{ change }', onblur='{ change }', value='{ model.value }', placeholder='{ model.placeholder }')

